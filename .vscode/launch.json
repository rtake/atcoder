{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "c++ gdb",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/problem.exe",
            "args": ["<", "${workspaceFolder}/problem.in"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
        }
    ]
}